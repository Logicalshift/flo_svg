# flo_svg

This is a Rust library that converts SVG files to the much easier to manipulate `flo_canvas` drawing
instructions. These can be rendered directly using the `flo_draw` crate, or used to generate rendering
instructions for other libraries.

